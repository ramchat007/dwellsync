[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  NODE_ENV = "production"
  NPM_FLAGS = "--include=dev --legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"

